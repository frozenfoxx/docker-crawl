apiVersion: v1
kind: Service
metadata:
  name: crawl
  labels:
    run: dkcrawl
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: crawl
  type: LoadBalancer
  sessionAffinity: ClientIP